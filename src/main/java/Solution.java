
public class Solution {

    public void algorithm_1(
            ConvexArea convexArea,
            Coordinate point0,
            Coordinate point1
    ){
        int directionX = point0.getX() > point1.getX() ? -1 : 1;
        int directionY = point0.getY() > point1.getY() ? -1 : 1;


    }
}
